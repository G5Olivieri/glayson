<h1><%= @note.title %></h1>
<%= @note.body %>

<nav>
<% if policy(@note).update? %>
  <%= link_to 'Edit', edit_note_path(@note) %>
<% end %>
<% if policy(@note).destroy? %>
  <%= link_to 'Destroy', @note, data: {
    turbo_method: :delete,
    turbo_confirm: "Are you sure?"
  } %>
<% end %>
</nav>
